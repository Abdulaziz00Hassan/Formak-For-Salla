# Formak-Phase7-to-Launch-v2.md — من المرحلة 7 حتى الإطلاق الحي

> **هذا الملف يحل محل `Formak-Phase7-to-Launch.md` (v1).** ارفع هذا الملف مع `Formak-AI-Context-v2.md` و`Formak-Handoff.md` لأي محادثة/AI جديدة — الثلاثة معًا يغطون كل شيء.
> يضيف هذا الملف عن v1: برومبت جاهز لكل خطوة برمجية في المراحل 8-11 (وليس المرحلة 7 فقط)، معايير فشل صريحة (لا نجاح فقط)، وتصحيحين تقنيين اكتُشفا بالبحث المباشر في توثيق سلة الرسمي (تفصيل في المرحلة 9).

---

## 0) الحالة الحالية

| الجزء | الحالة |
|---|---|
| Backend logic (webhook + regex + عزل multi-tenant + 3 إصلاحات) | ✅ مؤكد بأدلة فعلية |
| واتساب — التوكن الدائم + استلام رسالة حقيقية (hello_world) | ✅ مؤكد |
| إرسال واتساب عبر القالب النهائي `order_for_designer` | ❌ غير مختبر — كل شيء STUB حتى الآن |
| `/dashboard/mappings` | ❌ لم تُبنَ — **المرحلة 7، ابدأ هنا** |
| قالب `order_for_designer` معتمد من Meta | ❌ لم يُقدَّم |
| توثيق حساب الشركاء بسلة | ⚪ غير مؤكد — تحقق منه قبل المرحلة 9 |
| OAuth حقيقي مع متجر صديقك | ❌ لم يبدأ |
| نشر Vercel | ❌ لم يبدأ |
| اختبار حي على متجر صديقك | ❌ لم يبدأ — هذا هو التأكيد النهائي الوحيد الذي يهم |

---

## القاعدة العامة — طبّقها في كل خطوة قبل تصديق أي "تم"

مستخرجة من 6 من 7 أخطاء وقعت فعليًا في هذا المشروع (Formak-Handoff.md §3):

1. **لا "تم" بلا دليل تنفيذ فعلي.** لوج، استعلام SQL حقيقي، أو لقطة شاشة — لا جملة من Trae.
2. **افتح الكود، لا الملخص.** اطلب السطر الفعلي الذي تغيّر، لا وصفًا لما حدث.
3. **قارن كل حالة اختبار على حدة**، لا نتيجة إجمالية. إصلاح جزئي يبدو أحيانًا كإصلاح كامل في ملخص عام.
4. **أي استعلام تحقق يجب أن يقرأ العمود الصحيح فعليًا** — طابق اسم العمود بالمخطط في Formak-AI-Context-v2.md §3 بنفسك.
5. **لا تستخدم أي رقم لم تتحقق منه مباشرة** (تكلفة، سوق، إحصائية) في أي قرار.
6. **غياب حقل متوقع في نتيجة API قد يكون قصورًا في العرض لا فشلًا حقيقيًا** — تحقق قبل افتراض عطل.

---

## المرحلة 7: واجهة `/dashboard/mappings`

**الهدف:** CRUD لجدول `product_designer_map` + أول تفعيل فعلي لـRLS.

### البرومبت

```
اقرأ @Formak-AI-Context-v2.md. الـBackend جاهز ومختبَر بالكامل. ابنِ الآن /dashboard/mappings (وليس /dashboard/forms — احذف الأخير كليًا إن كان لا يزال موجودًا):

1. DataTable بـshadcn/ui يعرض صفوف product_designer_map: (اسم المنتج، معرف المنتج بسلة، اسم المصمم، رقم واتساب المصمم، أزرار تعديل/حذف).
2. نموذج (Dialog) لإضافة/تعديل تعيين: منتج (نص/رقم يدوي الآن، لاحقًا من Salla API)، اسم مصمم، رقم واتساب (تحقق من صيغة دولية: يبدأ بـ966 ولا يحتوي +).
3. Server Actions لعمليات CRUD على product_designer_map عبر Service Role Key فقط (server-side).
4. فعّل RLS على product_designer_map وorder_routing_log الآن (كانا معطّلين للتجربة المحلية)، بسياسة: التاجر يرى فقط صفوف merchant_id الخاص به.
5. اشرح تحديدًا: بأي آلية تربط سياسة RLS بين المستخدم المسجّل دخوله (session) و merchant_id؟ (JWT claim مخصص؟ جدول ربط؟ غيره؟) اعرض نص CREATE POLICY كاملاً — أحتاج هذا لأختبر العزل بشكل صحيح.
6. RTL كامل، بلا any، بلا useEffect لجلب البيانات.

أخبرني بالملفات التي أنشأتها/عدّلتها، نتيجة tsc --noEmit كاملة (الناتج الخام، لا ملخصًا)، ونص سياسة RLS كاملاً.
```

### ✅ معايير النجاح
- [ ] `tsc --noEmit` = Exit Code 0 — الصق المخرجات كاملة.
- [ ] إضافة/تعديل/حذف تعيين يعمل فعليًا — لقطة شاشة لكل عملية.
- [ ] Trae عرض نص `CREATE POLICY` الفعلي، وأوضح آلية ربط session↔merchant_id بدقة (وليس عبارة عامة مثل "عبر RLS").
- [ ] اختبار عزل RLS فعليًا نجح — استخدم الاستعلام أدناه.

**استعلام لإنشاء تاجر وهمي ثانٍ (Supabase SQL Editor):**
```sql
insert into merchants (salla_store_id, access_token, refresh_token, token_expires_at)
values (999999, 'dummy-token', 'dummy-refresh', now() + interval '1 year')
returning id;
-- احفظ الـid الناتج، أضف صفًا في product_designer_map بهذا merchant_id،
-- وتأكد أن حسابك الحالي (merchant_id مختلف) لا يراه في /dashboard/mappings
```

### ❌ علامات فشل / تحذيرات
- Trae يقول "فعّلت RLS" بلا عرض نص `CREATE POLICY` الفعلي → لا تصدّق، اطلبه نصًا كاملاً.
- أي استخدام لـ`SUPABASE_SERVICE_ROLE_KEY` داخل ملف بادئته `'use client'` → ابحث عنه بنفسك في الكود، هذا تسريب أمني مباشر.
- رقم واتساب يُقبل بصيغة خاطئة (بـ+ أو بدون 966) → جرّب إدخالاً خاطئًا عمدًا، تأكد من الرفض.

---

## المرحلة 8: قالب واتساب معتمد + إصلاح المتغيرات الفارغة

**الهدف:** قالب Meta معتمد + كود يبني متغيرات القالب بأمان + STUB معطّل.

### أ) خطوات يدوية (نفّذها أولاً، بالتوازي مع البرومبت الأول — لا تنتظرها)

**التوكن الدائم** (مؤكد يعمل سابقًا، تكرار للسياق فقط): Meta Business Manager → Business Settings → Users → System Users → أنشئ System User → اربطه بصلاحية `whatsapp_business_messaging` → ولّد Token → **Never expire** → `.env.local` كـ`WHATSAPP_PERMANENT_TOKEN`.

**القالب:** Meta Business Manager → WhatsApp Manager → Message Templates → إنشاء:
- الاسم: `order_for_designer`
- الفئة: **Utility** (وليس Marketing — فرق سعري كبير، ~80-90%)
- النص:
  ```
  🔔 طلب جديد #{{1}} — المنتج: {{2}}
  الملاحظة: {{3}}
  {{4}}
  ```
- قدّم للمراجعة الآن (ساعات إلى يومين عادة).

### ب) البرومبت الأول — لا ينتظر موافقة Meta، نفّذه فورًا

```
اقرأ @Formak-AI-Context-v2.md و @Formak-Handoff.md. عدّل منطق بناء متغيرات قالب واتساب (على الأرجح whatsapp-cloud.ts — تحقق من الاسم الفعلي وأخبرني إن كان مختلفًا):

1. المتغير {{3}} (الملاحظة): إن كان raw_note فارغًا أو null أو undefined، مرّر النص الثابت "لا توجد ملاحظات" بدلاً منه. لا تمرر أبدًا سلسلة فارغة — واتساب يرفض القالب أو يشوّهه.
2. المتغير {{4}} (شارة التخصيص): مرّر "⚠️ يحتوي على تخصيص باسم" فقط إذا كان personalizationDetected = true. غير ذلك، مرّر "—" (شرطة واحدة، وليس سلسلة فارغة تمامًا).
3. لا تلمس منطق الإرسال أو STUB الآن — فقط دالة بناء المتغيرات.
4. اكتب اختبار وحدة بسيط لهذه الدالة يغطي 4 حالات: ملاحظة+تخصيص مكتشف، ملاحظة بلا تخصيص، بلا ملاحظة+تخصيص، بلا ملاحظة+بلا تخصيص.

أخبرني: اسم الملف الفعلي، كود دالة بناء المتغيرات كاملاً، ونتيجة الاختبارات الأربعة تحديدًا (أي منها نجح/فشل، بلا تلخيص).
```

### ج) البرومبت الثاني — فقط بعد اعتماد Meta للقالب

```
القالب order_for_designer اعتُمد من Meta. 

1. أضف WHATSAPP_TEMPLATE_NAME=order_for_designer إلى .env.local.
2. ابحث في كل الكود (خصوصًا اختبارات order-processor) عن كل مكان مفعّل فيه STUB لإرسال واتساب، واعرضه لي قبل أي تعطيل.
3. بعد موافقتي، عطّله بحيث يذهب الإرسال فعليًا إلى WhatsApp Cloud API الحقيقي.
4. اكتب سكربت اختبار واحد بسيط يرسل رسالة تجريبية لرقم هاتفي الشخصي فقط: [ضع رقمك هنا بصيغة 966xxxxxxxxx] — وليس لأي رقم مصمم حقيقي. طلب وهمي واحد كافٍ، لا تشغّل Full Run بالحجم الكامل.

أخبرني بالسطر الذي عدّلته لتعطيل STUB، ونتيجة الإرسال التجريبي كاملة.
```

### ✅ معايير النجاح
- [ ] حالة القالب في Meta Business Manager = **Approved** (لقطة شاشة).
- [ ] 4/4 اختبارات دالة بناء المتغيرات نجحت — نتيجة كل حالة على حدة.
- [ ] رسالة تجريبية وصلت فعليًا لرقمك الشخصي بنص كامل وصحيح — **لقطة شاشة من واتساب نفسه**، ليس لوج السيرفر.
- [ ] Trae عرض السطر المحدد الذي عطّل به STUB.

### ❌ علامات فشل / تحذيرات
- القالب "Pending"/"In Review" → لا تنفّذ البرومبت الثاني إطلاقًا حتى الاعتماد.
- الرسالة وصلت لكن {{3}} أو {{4}} تظهر فارغة أو "undefined"/"null" حرفيًا → الإصلاح لم يُطبَّق فعليًا، ارجع للبرومبت الأول.
- Trae يقول "عطّلت STUB" بلا عرض أين كان مفعّلاً أصلاً → لا تصدّق.
- القالب مرفوض من Meta → غالبًا الفئة (Marketing بدل Utility) أو نص يبدو ترويجيًا. عدّل وأعد التقديم.

---

## المرحلة 9: OAuth حقيقي مع سلة

**الهدف:** ربط متجر صديقك فعليًا، تخزين merchant_id حقيقي.

### 🔍 تصحيحان اكتُشفا بالبحث المباشر في توثيق سلة الرسمي (docs.salla.dev) — لم يكونا في ملفاتك

**1. يوجد نمطان لـOAuth في سلة، لا نمط واحد:**

| | Easy Mode | Custom Mode (ما تخطط له) |
|---|---|---|
| كيف يصل التوكن | تلقائيًا عبر webhook — تستمع لحدث `app.store.authorize` على نفس Webhook URL لتطبيقك | تبنيه يدويًا: رابط توجيه + مسار callback يستقبل `code` |
| كود مطلوب | أقل — يعيد استخدام نمط webhook handler الموجود عندك أصلاً | أكثر — مسارين جديدين |
| القيد | Easy Mode **إلزامي** فقط للتطبيقات المنشورة على متجر تطبيقات سلة | متاح بلا قيد لتطبيق Private App مثل تطبيقك |

خطتك الأصلية (Custom Mode) صالحة تمامًا ولا داعي لتغييرها إن كنت مرتاحًا لها — لكن Easy Mode كان يعني كودًا أقل وسطح أخطاء أصغر لأنه يعيد استخدام webhook handler الموجود. البرومبت أدناه لـCustom Mode كما هو مخطط. إن أردت التبديل لـEasy Mode بدلاً منه، أخبرني واكتب برومبتًا منفصلاً.

**2. قواعد صارمة حول التوكنات لم تكن موثقة عندك:**
- `access_token` صالح **14 يومًا فقط**.
- `refresh_token` صالح شهرًا واحدًا، و**للاستخدام مرة واحدة فقط** — استخدامه مرتين يُبطل كل التوكنات المرتبطة به فورًا ويتطلب من صديقك **إعادة تثبيت التطبيق بالكامل** من جديد لاستعادة الوصول.
- `scope` يجب أن يتضمن `offline_access` تحديدًا، وإلا **لن يصلك refresh_token إطلاقًا** — وعمود `refresh_token` عندك `NOT NULL`، فسيفشل إدراج الصف بصمت أو بخطأ غير واضح.

**⚠️ فجوة لم تُخطط لها بعد في المراحل 7-11:** لا توجد خطوة لبناء تجديد access_token التلقائي (سيموت بعد 14 يومًا). هذا خارج نطاق طلبك الحالي، لكن اعرف أن الأداة ستتوقف عن العمل صامتة بعد أسبوعين من نجاح المرحلة 11 ما لم تجهّز آلية تجديد (cron job أو serverless function مجدولة) لاحقًا.

### ⛔ بوابة قبل البدء (Gate 1)
تأكد أن حالة توثيق حساب الشركاء بسلة **"Approved" فعليًا الآن** في بوابة الشركاء — لا تبدأ بافتراض ذلك.

### ⛔ بوابة قبل الاختبار الحقيقي فقط (Gate 2 — ليست قبل كتابة الكود)
الخطوة الأخيرة من هذه المرحلة (تفويض حقيقي من صديقك) تحتاج **رابط HTTPS حي** يستقبل الـcallback. هذا يعني أنك تحتاج نشرًا أوليًا على Vercel (ولو ناقصًا، فقط كافيًا لتشغيل هذا المسار) **قبل** إكمال هذه المرحلة، أو استخدام نفق محلي مؤقت مثل ngrok. لا يمكن اختبار OAuth حقيقي مع صديقك على `localhost` مباشرة. راجع المرحلة 10 لتفاصيل النشر الكاملة — يمكنك نشر نسخة أولية سريعة الآن وإكمال باقي متغيرات البيئة لاحقًا.

### البرومبت

```
اقرأ @Formak-AI-Context-v2.md. ابنِ الآن Custom Mode OAuth مع سلة، مسارين:

1. مسار توليد رابط التوجيه (يمكن أن يكون route بسيط أو دالة تُستخدم يدويًا):
   https://accounts.salla.sa/oauth2/auth?client_id={SALLA_CLIENT_ID}&response_type=code&redirect_uri={SALLA_CALLBACK_URL}&scope=offline_access&state={قيمة عشوائية، خزّنها مؤقتًا للتحقق في الخطوة التالية}
   ⚠️ scope يجب أن يكون offline_access بالضبط — بدونها لن يصل refresh_token وسيفشل إدراج الصف في merchants (العمود NOT NULL).

2. /api/auth/callback: يستقبل query params code، scope، state من سلة.
   - تحقق أن state يطابق القيمة المخزّنة سابقًا (حماية CSRF بسيطة). إن لم يطابق، ارفض بخطأ واضح.
   - إن لم يوجد code (رفض المستخدم التفويض): تعامل مع الحالة بلا كراش، أعد توجيهًا لصفحة خطأ مفهومة.
   - بادلة code بـaccess_token/refresh_token عبر POST إلى https://accounts.salla.sa/oauth2/token (server-side فقط، لا تكشف SALLA_CLIENT_SECRET أبدًا في كود client-side).
   - خزّن النتيجة في جدول merchants (salla_store_id, access_token, refresh_token, token_expires_at). احسب token_expires_at بإضافة 14 يومًا من الآن ما لم يرجّع الرد expires_in فعليًا — استخدمه إن وُجد.
   - لا تبنِ آلية تجديد تلقائي الآن — فقط تخزين صحيح. سنتعامل مع التجديد لاحقًا في مرحلة منفصلة.

أخبرني بالملفين الناتجين كاملين، ونتيجة tsc --noEmit.
```

### خطوات يدوية
1. Salla Partners → إنشاء Private App جديد → احصل على `Client ID` و`Client Secret` → `.env.local`.
2. في إعدادات التطبيق: اختر **Custom Mode**، وأدخل `SALLA_CALLBACK_URL` كـcallback URL المسجّل.
3. بعد Gate 2 (رابط حي متاح): اطلب من صديقك رابط متجره، ثم إما أرسل طلب وصول عبر بوابة الشركاء أو استخدم رابط التثبيت المباشر `https://s.salla.sa/apps/install/{app-id}` وأرسله لصديقك مباشرة — الأبسط عمليًا.
4. بعد موافقته: نفّذ تدفق OAuth كاملاً مرة واحدة يدويًا، وتأكد أن merchant_id الحقيقي خُزّن (استعلم مباشرة).

```sql
select id, salla_store_id, token_expires_at from merchants order by created_at desc limit 5;
```

### ✅ معايير النجاح
- [ ] `Client ID`/`Client Secret` في `.env.local` فقط، لا في أي كود client-side.
- [ ] `tsc --noEmit` = Exit Code 0.
- [ ] تدفق OAuth كامل نُفّذ فعليًا مع متجر صديقك (لا بيانات وهمية).
- [ ] استعلام `SELECT` أعلاه يعرض صفًا حقيقيًا بـ`salla_store_id` مطابق لمتجر صديقك، و`refresh_token` **غير فارغ**.
- [ ] `token_expires_at` منطقي (بعد 13-14 يومًا من الآن تقريبًا).

### ❌ علامات فشل / تحذيرات
- `refresh_token` فارغ أو null في الصف المخزَّن → غالبًا `scope=offline_access` لم يُرسَل فعليًا في رابط التوجيه — افحص الرابط الذي تولّد بالضبط.
- الـcallback لم يُستدعَ إطلاقًا → `SALLA_CALLBACK_URL` غير مطابق حرفيًا لما سُجّل في إعدادات التطبيق على بوابة الشركاء (حتى / زائدة تكسره).
- أي محاولة اختبار تستخدم refresh_token مرتين (حتى بالخطأ، مثل ضغط زر إعادة المحاولة مرتين بسرعة) → يُبطل كل شيء، صديقك يحتاج إعادة تثبيت التطبيق من الصفر. لا تجرّب منطق تجديد "للتأكد" في هذه المرحلة.

---

## المرحلة 10: نشر Vercel

**الهدف:** الكود حي على Vercel، كل متغيرات البيئة صحيحة، الرابط يستجيب.

> **تذكير:** قد تكون نشرت نسخة أولية بالفعل لأجل Gate 2 في المرحلة 9. هذه المرحلة تكمّل ذلك بكل المتغيرات والفحوصات، لا تبدأ من صفر بالضرورة.

### البرومبت (فحص أمان قبل الدفع)

```
اقرأ @Formak-AI-Context-v2.md. قبل الدفع لـVercel (أو إكماله)، افحص المشروع بالكامل:

1. ابحث في كل ملفات الكود (بما فيها الاختبارات) عن أي secret مكتوب مباشرة كنص بدل قراءته من process.env — تحديدًا: SALLA_CLIENT_SECRET، SUPABASE_SERVICE_ROLE_KEY، WHATSAPP_PERMANENT_TOKEN، أي webhook secret. أخبرني بأي سطر وجدته فيه (ملف + رقم سطر)، أو صرّح صراحة أنك بحثت ولم تجد شيئًا (اذكر أمر البحث المستخدم).
2. الصق محتوى .gitignore كاملاً، وتأكد أن .env.local مدرج فيه.
3. شغّل npm run build محليًا (أو الأمر المكافئ في package.json) وأخبرني بالنتيجة الكاملة.
4. ابحث عن أي console.log يطبع قيمة توكن أو secret كاملة، واحذفه أو استبدله بطباعة جزئية.

أخبرني بنتيجة كل نقطة بالتفصيل. نقطة 1 لا تُقبل فيها "لا يوجد" بلا ذكر أمر البحث الفعلي المستخدم.
```

### خطوات يدوية
1. ادفع الكود لمستودع GitHub. **تحقق بنفسك مباشرة على صفحة GitHub بعد الدفع** أن `.env.local` لم يُرفَع — لا تكتفِ بالثقة في `.gitignore`.
2. اربط المستودع بـVercel.
3. أضف متغيرات البيئة:

| المتغير | من أين | سري؟ |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase → Project Settings → API | لا |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase → Project Settings → API | لا |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Project Settings → API | **نعم** |
| `SALLA_CLIENT_ID` | Salla Partners → تطبيقك | لا |
| `SALLA_CLIENT_SECRET` | Salla Partners → تطبيقك | **نعم** |
| `SALLA_WEBHOOK_SECRET` | إعدادات Webhook بتطبيق سلة | **نعم** |
| `SALLA_CALLBACK_URL` | `https://your-app.vercel.app/api/auth/callback` | لا |
| `WHATSAPP_PERMANENT_TOKEN` | من المرحلة 8 | **نعم** |
| `WHATSAPP_PHONE_NUMBER_ID` | Meta Business Manager | لا |
| `WHATSAPP_TEMPLATE_NAME` | `order_for_designer` | لا |

### ✅ معايير النجاح
- [ ] Trae أكّد بدليل بحث فعلي (أمر البحث نفسه) أنه لا يوجد secret مكتوب مباشرة بالكود.
- [ ] `.env.local` غير موجود على GitHub — تحقّق بنفسك من صفحة المستودع مباشرة.
- [ ] `npm run build` نجح محليًا بلا أخطاء قبل الدفع.
- [ ] كل الـ10 متغيرات مضافة في Vercel — عدّها بنفسك.
- [ ] `https://your-app.vercel.app/api/salla/webhook` يرد بنجاح على GET.

### ❌ علامات فشل / تحذيرات
- Build ينجح محليًا لكن يفشل على Vercel → راجع Vercel Build Logs مباشرة (غالبًا متغير ناقص أو فرق Node version)، لا تخمّن السبب.
- الرابط يرد 404 → مسار أو اسم ملف خطأ، أو الدفع لم يكتمل فعليًا.
- **أي secret ظهر في أي commit سابق على GitHub (حتى لو حُذف لاحقًا) → غيّر قيمته فورًا من مصدره (Supabase/Salla/Meta)**، لأن الـcommit القديم يبقى مرئيًا في history حتى بعد الحذف.

---

## المرحلة 11: الإطلاق الحي

**الهدف:** طلب حقيقي في متجر صديقك ينتج رسالة واتساب حقيقية تصل للمصمم الصحيح.

### قبل البدء
- [ ] STUB معطّل فعليًا (مرحلة 8).
- [ ] Webhook مُنشأ في سلة (من لوحة التحكم أو Apps API) يشير لـ`https://your-app.vercel.app/api/salla/webhook`، مشترك في `order.created`.
- [ ] تعيين واحد على الأقل في `/dashboard/mappings` لمنتج حقيقي في متجر صديقك.

### برومبت اختياري — فقط إن كانت اللوجات الحالية غير كافية للتتبع
```
اقرأ @Formak-AI-Context-v2.md. قبل الاختبار الحي، أضف console.log مختصر عند كل نقطة قرار رئيسية في مسار webhook: استلام الطلب، نتيجة البحث في product_designer_map (وُجد/لم يوجد)، نتيجة regex (تخصيص مكتشف/لا)، حالة إرسال واتساب (نجح/فشل مع السبب الكامل من رد API). لا تطبع أي secret أو توكن كامل.
```

### الاختبار الحي (بشري بالكامل، لا برومبت هنا)
اطلب من صديقك تنفيذ طلبين تجريبيين حقيقيين:
1. منتج له تعيين مصمم، مع ملاحظة تحتوي اسمًا (مثال: "بأسم خالد").
2. نفس المنتج أو آخر له تعيين، **بلا أي ملاحظة إطلاقًا**.

راقب Vercel Logs مباشرة أثناء الطلبين.

### ✅ معايير النجاح
- [ ] Webhook وصل فعليًا (200 OK فورًا في Vercel Logs) — للطلبين كليهما.
- [ ] سجل جديد في `order_routing_log` لكل عنصر:
  ```sql
  select * from order_routing_log order by created_at desc limit 5;
  ```
- [ ] الطلب 1 (باسم بالملاحظة): `personalization_detected = true`، `extracted_name` يحتوي الاسم الفعلي.
- [ ] الطلب 2 (بلا ملاحظة): `whatsapp_status = 'sent'` أيضًا — **هذا تحديدًا هو الخطأ #2 من سجل الأخطاء، تأكد أنه لا يزال مُصلَحًا فعليًا في بيئة حية**.
- [ ] **تأكيد من المصمم نفسه** (لقطة شاشة من واتسابه هو) أن الرسالة وصلت بنص صحيح كامل — ليس لوج السيرفر فقط.

### ❌ علامات فشل / تحذيرات
- Webhook لا يصل إطلاقًا → تحقق أن الرابط في إعدادات Webhook بسلة صحيح حرفيًا، وأن `SALLA_WEBHOOK_SECRET` في Vercel يطابق تمامًا ما في لوحة سلة.
- الطلب بلا ملاحظة لم يُرسَل له واتساب → رجوع لخطأ #2/#4 من السجل، لم يُصلَح فعليًا رغم الاختبارات السابقة على بيانات محلية.
- الرسالة وصلت بمتغيرات فارغة/undefined → المرحلة 8 لم تُختبر بما يكفي قبل هذه المرحلة.
- `whatsapp_status = 'failed'` → افحص استجابة API الكاملة في اللوج، لا تكتفِ بغياب حقل واحد (راجع القاعدة العامة رقم 6).

---

## ملحق: نموذج التقرير النهائي

```
المرحلة 7: [تم/لم يتم] — [آلية ربط RLS بـsession] — [نتيجة اختبار العزل]
المرحلة 8: [حالة القالب: معتمد/قيد المراجعة/مرفوض] — [نتيجة اختبار الإرسال الحقيقي لرقمك]
المرحلة 9: [Custom/Easy Mode المستخدم فعليًا] — [merchant_id حقيقي مخزّن؟] — [نتيجة SELECT]
المرحلة 10: [رابط Vercel] — [كل المتغيرات الـ10 مضافة؟ نعم/لا]
المرحلة 11: [نص الطلبين التجريبيين] — [لقطة Vercel Logs] — [تأكيد المصمم استلام الرسالة: نعم/لا]
```
