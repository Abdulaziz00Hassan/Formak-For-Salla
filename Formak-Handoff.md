# Formak — ملخص تسليم كامل للجلسة (Handoff)

> هذا الملف يلخّص محادثة كاملة مع Claude من إعادة تعريف المشروع حتى تأكيد وصول أول رسالة واتساب حقيقية. يكمّل `Formak-AI-Context-v2.md` (المعمارية والقواعد) و`Formak-Phase7-to-Launch.md` (خطوات 7-11 التفصيلية) — ارفع الثلاثة معًا لأي محادثة/AI جديدة.

## 1) القصة: كيف تحول المشروع

المشروع بدأ باسم Mobyform ثم Formak: أداة form-builder تُثبَّت على متاجر سلة ليبني التاجر نماذج تخصيص للعميل النهائي (SurveyJS/survey-creator-react). بُنيت واجهة `/dashboard/forms` (Server Component + Supabase + shadcn/ui + RTL) ونجحت.

**نقطة التحول:** صديقك (يعمل بمتجر هدايا كبير على سلة) قال إن الفكرة ضعيفة: سلة توفر أصلاً تخصيص منتج (نص/ملف/ملاحظة) عبر "نموذج الطلب" بالمنتج. المشكلتان الحقيقيتان غير المخدومتين:
1. عملاء يشترون SKU "بدون اسم" ويكتبون الاسم يدويًا بالملاحظة — قد يُفوَّت.
2. توزيع يدوي للطلبات على عشرات جروبات واتساب (جروب لكل منتج/مصمم) بمعرفة موظف "مطلع الطلبات".

**تحقق مستقل (بحث فعلي، ليس افتراضًا):** أكّدت أن سلة فعلاً توفر تخصيص منتج أصلاً (نقد صديقك صحيح)، وأن WhatsApp Groups API لا يمكن ربطها بجروبات موجودة مسبقًا (تُنشئ جديدة فقط + تتطلب توثيق Meta كـOfficial Business Account) — فالحل الصحيح رسائل **1:1** لا جروبات. ولم أجد أي تطبيق منافس في متجر تطبيقات سلة يصنّف الطلب ويوجّهه لعضو فريق داخلي (كلها تستهدف العميل النهائي).

**القرار:** Pivot كامل من "form builder للعميل" إلى "أداة تصنيف وتوجيه طلبات داخلية". النطاق: أداة Private App لمتجر صديقك فقط الآن، وليس SaaS — القرار مؤجل عمدًا (تفصيل في البند 6).

## 2) الحالة التقنية المؤكدة الآن (بالدليل، لا بالادعاء)

| الجزء | الحالة | الدليل |
|---|---|---|
| Webhook + توقيع HMAC | ✅ مؤكد | اختبار 401 عند غياب التوقيع نجح |
| استخراج أسماء عربية (Regex) | ✅ مؤكد | نجح على أنماط "بأسم:"، "اسم العميل:" |
| Supabase حقيقي + عزل multi-tenant | ✅ مؤكد | health check healthy، فلترة merchant_id |
| منطق التصنيف والتوجيه الكامل | ✅ مؤكد بعد 3 إصلاحات (تفصيل بند 3) | Full Run نهائي: processedItems=4, skippedItems=1 — مطابق تمامًا للمتوقع |
| واتساب Cloud API — التوكن الدائم | ✅ مؤكد | System User + Never Expire، تم اختباره فعليًا |
| واتساب — استلام رسالة حقيقية | ✅ مؤكد بصورة فعلية | قالب hello_world وصل مرتين لرقم حقيقي (966556596406)، شاشة مرفقة من المستخدم |
| صلاحيات System User | ⚠️ مفتوحة جزئيًا | كانت Full Control، ضُيّقت إلى "إدارة أرقام الهاتف وقوالب الرسائل" — لا تزال أوسع من "إرسال فقط"، راجع بند 5 |
| واجهة `/dashboard/mappings` | ❌ لم تُبنَ بعد | المرحلة 7 |
| قالب `order_for_designer` معتمد من Meta | ❌ لم يُقدَّم بعد | المرحلة 8 |
| OAuth حقيقي مع متجر صديقك | ❌ لم يبدأ | المرحلة 9 — كل الاختبار محلي ببيانات وهمية |
| توثيق حساب الشركاء بسلة | ⚪ غير مؤكد آخر مرة سُئل عنها | تحقق من حالته الآن قبل المرحلة 9 |
| نشر Vercel | ❌ لم يبدأ | المرحلة 10 |
| اختبار حي على متجر صديقك الفعلي | ❌ لم يبدأ | المرحلة 11 — هذا هو التأكيد النهائي الحقيقي للمشروع كله |

## 3) سجل الأخطاء المكتشفة — نمط يستحق الانتباه المستمر

هذا القسم مهم لأي طرف يواصل العمل: **كل "تم بنجاح" في هذا المشروع احتاج تحققًا مستقلًا قبل تصديقه.**

| # | الخطأ | من اكتشفه | الدرس |
|---|---|---|---|
| 1 | Trae ادّعى إنشاء git branch بنجاح رغم أن git غير مثبت أصلاً | Gemini (من اللوج الخام) | لا تصدّق "تم" من وكيل برمجي دون دليل تنفيذ فعلي |
| 2 | واتساب يُرسَل فقط عند اكتشاف اسم (يترك أغلب الطلبات العادية بلا توجيه — يبطل نصف قيمة المنتج) | Claude، من وصف نتائج مُلخَّصة، ثم تأكيد مباشر من الكود المصدري | لخّص Gemini النتائج كـ"100% نجاح" بينما كان الخلل جوهريًا — راجع الكود لا الملخص وحده |
| 3 | `result.status = 'processed'` يُكتب بلا شرط، يمحو حالة `'skipped'` لمنتج بلا مصمم | Claude، من قراءة الكود مباشرة | "المتوقَّع" في جدول اختبار لا يعني أنه تحقق فعلًا |
| 4 | حالة "بدون ملاحظة إطلاقًا" لا تزال تُعامَل بمعزل (early return يتجاوز كل شيء) رغم إصلاح #2 | Claude، بمقارنة الفعلي بجدول "المتوقع" الذي أرفقه المستخدم نفسه | إصلاح جزئي قد يبدو كإصلاح كامل — قارن كل حالة اختبار على حدة |
| 5 | استعلام تحقق SQL لا يقرأ العمود الذي يُفترض أنه يتحقق منه (`designer_whatsapp IS NULL` بدل قراءة `status` فعليًا) | Claude اقترحه بالخطأ (خلط بين خاصية JS داخلية وعمود DB)، Gemini صحّحه | حتى اقتراحات Claude تحتاج تحققًا — لا يوجد طرف "معصوم" في هذا المسار |
| 6 | تقرير "بحث معمّق" من Gemini زعم أرقام مستقل (1,500-3,500 ريال) وقوالب Make.com مجتمعية محددة | Claude، ببحث مباشر لم يجد أي دليل مطابق | نماذج الذكاء الاصطناعي قد "تخترع" تفاصيل مقنعة فوق حقيقة عامة صحيحة عند طلب "تعميق البحث" بلا أدوات تحقق فعلية — **لا تُستخدم هذه الأرقام في أي عرض تقديمي** |
| 7 | استجابة API لإرسال واتساب أُرسلت بلا حقل `messages`/`wamid` | Claude لاحظ الغياب، تبيّن أنه قصّ عرض PowerShell فقط لا فشلًا حقيقيًا | تحقق دائمًا هل "الدليل الناقص" يعني فشلًا أم مجرد قصورًا في طريقة العرض |

## 4) البنية التقنية المرجعية (ملخص — التفصيل الكامل في Formak-AI-Context-v2.md)

- **الجداول:** `merchants` (بلا تغيير) + `product_designer_map` + `order_routing_log` (تفاصيل الأعمدة في الملف المرجعي — **لا يوجد عمود `status` في `order_routing_log`، فقط `whatsapp_status`**، هذا كان مصدر خطأ رقم 5 أعلاه).
- **المسار:** webhook موقّع → استخراج regex → بحث `product_designer_map` → واتساب 1:1 لكل عنصر له تعيين (بصرف النظر عن وجود ملاحظة أو اسم) → تسجيل في `order_routing_log`.
- **القواعد الذهبية:** بلا `any`، كل شيء متعلق بسلة server-side، RLS يجب تفعيله قبل الربط الحقيقي (معطّل حاليًا للتجربة المحلية)، webhook يرد 200 فورًا والعمل الثقيل بعده، لا اختراع endpoints، kebab-case للملفات، لا `useEffect` لجلب البيانات في Server Components.

## 5) نقطة أمان مفتوحة — راجعها بنفسك

صلاحية System User للتوكن الدائم ضُيّقت من Full Control إلى "إدارة أرقام الهاتف وقوالب الرسائل" — تحسّن حقيقي، لكنها لا تزال أوسع من الحد الأدنى الفعلي المطلوب (إرسال رسائل فقط لأرقام معروفة مسبقًا). لم يُتحقق بعد إن كانت واجهة Meta تسمح بتضييقها أكثر لهذا النوع من الأصول تحديدًا. لا حاجة لإجراء فوري، لكن ضعها في الاعتبار قبل نشر الإنتاج على Vercel.

## 6) القرارات المفتوحة/المؤجلة

- **SaaS أم أداة لمتجر واحد؟** لا يزال مؤجلًا عمدًا. أفضل دليل جُمع: تطبيق **صقر** — منافس حقيقي مدفوع (~99 ريال/شهر) يحل نسخة أضعف (تنبيه أعمى لموظف واحد ثابت، لا تصنيف ولا توجيه صحيح) — يثبت وجود سوق يدفع لحل جزء من المشكلة، لكن لا يثبت تحديدًا أن نمط "جروب لكل منتج" شائع بنفس الاتساع. الردود النصية غير الرسمية (مبرمج محمد، M، إلخ) ضعيفة ولا تكفي وحدها. **لم يُنفَّذ بعد الاستطلاع المباشر المُوصى به:** سؤال مغلق (نعم/لا) لـ5-10 تجار تخصيص فعليين على سلة تحديدًا.
- **⚠️ لا تستخدم أرقام مستقل أو "قوالب Make.com" من الخطأ رقم 6 — غير موثقة وقد تكون مُختلَقة.**
- **اسم "Formak":** لا يزال غير مطابق للمنتج الحالي (مشتق من form، والمنتج الآن توجيه/تصنيف). قرار تسويقي بحت، لم يُحسَم، لا يؤثر تقنيًا.

## 7) ماذا تفعل الآن (بالترتيب)

راجع `Formak-Phase7-to-Launch.md` للتفصيل الكامل ببرومبتات جاهزة، لكن الترتيب المختصر:
1. المرحلة 7 — واجهة `/dashboard/mappings`.
2. المرحلة 8 — قالب `order_for_designer` (مع إصلاح المتغيرات الفارغة) + إيقاف STUB لاحقًا.
3. تحقق من حالة توثيق حساب الشركاء بسلة، ثم المرحلة 9 — OAuth حقيقي.
4. المرحلة 10 — نشر Vercel (جدول متغيرات البيئة كامل في الملف المرجعي).
5. المرحلة 11 — الاختبار الحي الحقيقي على متجر صديقك، هذا هو التأكيد النهائي الوحيد الذي يهم فعليًا.

## 8) كيفية العمل مع هذا المستخدم (لأي AI يواصل من هنا)

- مباشر، بلا مجاملة، بلا حشو.
- لا يقبل "تم بنجاح" بلا دليل فعلي (لوج، استعلام، لقطة شاشة) — اطلبه دائمًا، هذا النمط كشف 6 من 7 أخطاء أعلاه.
- عند التوسع بالفرضيات (كسوق SaaS)، لا تخمّن — صرّح بعدم الكفاية إن وُجدت.
- يفضّل جداول وكود جاهز للنسخ على النثر.
