# Formak-Handoff-4.md — تشخيص الفشل الصامت الكامل، جاهزية شبه كاملة، حاجز واحد متبقٍّ

> يُلغي هذا الملف `Formak-Handoff-3.md` بالكامل — لا ترفعه للمحادثة الجديدة، كل قيمته مدموجة ومُحدَّثة هنا. القرارات المُقفَلة بالقسم 0 لم تتغيّر، مُعاد ذكرها للاكتمال فقط.
>
> لا يزال ساريًا بلا تعديل: `Formak-Ai-Context-v3.md` (المرجع التقني الثابت)، وقسما المرحلتين 10-11 من `Formak-Phase7-to-Launch-v2.md` (معظمهما منجَز فعليًا الآن — التفاصيل بالقسم 1).
>
> **الفارق الجوهري عن كل الملفات السابقة:** هذه أول مرة تُثبَت فيها السلسلة كاملة تعمل من طرفها لطرفها بطلب **حقيقي** من متجر سلة التجريبي (لا حمولة مصطنعة) — استقبال → تصنيف → توجيه → إرسال واتساب → **تأكيد تسليم فعلي**. الحاجز الوحيد المتبقي أمام إطلاق حقيقي على متجر صديقك ليس تقنيًا بالمعنى البرمجي، بل قرار ربط رقم واتساب حقيقي.

---

## 0) قرارات مُقفَلة (بلا تغيير عن Handoff-3)

- **الهيكل القانوني:** صديقك المالك الفعلي (حساب شركاء سلة منفصل باسمه)، أنت "عضو الشركة" لا "مدير". أجر ثابت، لا نسبة أرباح، المال لا يمر عبرك. الاتفاق المكتوب **لا يزال غير مؤكَّد التوثيق** — تحقّق.
- **Custom Mode OAuth** (لا Easy Mode) — نهائي.
- **فئة قالب واتساب `order_for_designer`: Marketing** (لا Utility) — مؤكَّد من صديقك، لا يُعاد فتحه.
- **لغة القالب المعتمدة فعليًا: `ar_AE`** — اكتُشف هذه الجلسة (القسم 5، خطأ #24)، وليس `ar` الافتراضي كما كان مفترَضًا. النص العربي الكامل موثَّق بالقسم 2.
- SaaS مقابل أداة لمتجر واحد: لا يزال مؤجَّلًا عمدًا.

---

## 1) الحالة التقنية المؤكدة الآن (بدليل تنفيذ فعلي، لا افتراض)

| البند | الحالة | الدليل |
|---|---|---|
| استقبال `order.created` **حقيقي** من سلة (لا مصطنع) | ✅ | `User-Agent: Salla Webhook v2.0`، طلبين حقيقيين من لوحة test_01 (إنشاء + دفع بنكي)، كلاهما 200 خلال ثوانٍ |
| استخراج `salla_product_id` (متداخل `item.product.id`) | ✅ | مؤكَّد بطلبات حقيقية متعددة، `salla_product_id` صحيح بكل مرة |
| قراءة الملاحظة (`item.notes`، جمع) | ⚠️ **جزئي** | يعمل فقط حين النص بحقل الملاحظة العام — **لا يعمل** حين يُدخله العميل عبر "خيار منتج" (القسم 4، خطأ #30) |
| معالجة خلفية بلا فقدان (await، لا fire-and-forget) | ✅ | Vercel كان يقتل المعالجة بعد الرد 200 — أُصلح، الآن يتم الرد بعد اكتمال المعالجة فعليًا (3-5 ثوانٍ) |
| تجديد `access_token` تلقائي (Cron) | ✅ **مختبَر حيًا** | قفزة 14 يومًا حقيقية موثَّقة (`token_expires_at` تحقَّق حسابيًا من `Date` header مقابل قاعدة البيانات) |
| إرسال واتساب حقيقي عبر القالب المعتمد | ✅ | وصل فعليًا لهاتف حقيقي، مؤكَّد بصريًا (لا لوج فقط) |
| **رؤية حالة تسليم واتساب** (webhook منفصل جديد) | ✅ **مبني ومختبَر حيًا** | التقط حالتي `read` (نجاح) و`failed` (مع كود خطأ Meta 131049 الحقيقي) بدقة تامة |
| شارة `{{4}}` بالعربية (بدل "None"/إنجليزي) | ✅ **منجَز ومدفوع** | مراجَع سطرًا بسطر، البناء نجح، push مؤكَّد من المستخدم |
| **رقم واتساب حقيقي (غير Sandbox)** | ❌ **الحاجز الوحيد المتبقي** | القسم 3 |
| استخراج الاسم من "خيار منتج" (لا الملاحظة فقط) | ❌ مفتوح | القسم 4 |
| اتفاق العمل المكتوب مع صديقك | ❌ غير مؤكَّد | غير مُطارَد هذه الجلسة |
| توضيح عمود `whatsapp_number` بجدول `merchants` | ❌ مفتوح | غير مُطارَد هذه الجلسة |

---

## 2) المعمارية الفعلية المُحدَّثة (ما استُحدث هذه الجلسة تحديدًا)

**آلية تجديد التوكن** — `app/api/cron/refresh-salla-tokens/route.ts`، مجدول يوميًا 3ص UTC عبر `vercel.json` (`crons`). مصادقة عبر `Authorization: Bearer ${CRON_SECRET}` — **`CRON_SECRET` متغيّر بيئة عادي يُنشَأ يدويًا، لا يُوفَّره Vercel تلقائيًا** (تصحيح لمعلومة خاطئة أعطيتها بجلسة سابقة). نافذة تحديث = `token_expires_at < now() + 3 days`، تكفي وحدها كـmutex فعلي (بعد التحديث تقفز القيمة 14 يومًا فتخرج فورًا من النافذة، فلا تكرار ممكن ماديًا لاستخدام `refresh_token`).

**آلية حالة تسليم واتساب** — `app/api/whatsapp/webhook/route.ts` (جديد كليًا)، مسجَّل بلوحة Meta Configuration → Webhook، مشترك بحقل `messages`. مصادقة `X-Hub-Signature-256` (HMAC SHA256 بـ`WHATSAPP_APP_SECRET`، نفس نمط توقيع سلة). يحدّث `order_routing_log.whatsapp_delivery_status`/`whatsapp_delivery_error` بمطابقة `whatsapp_message_id`. الأعمدة الثلاثة (`whatsapp_message_id`, `whatsapp_delivery_status`, `whatsapp_delivery_error`) أُضيفت يدويًا بـSupabase SQL Editor مباشرة، لا عبر كود.

**env vars الفعلية بالكود (تختلف عن التسمية المخطَّطة أصلًا بـPhase 10 — القسم 5، خطأ #23):**
`WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_DESIGNER_NOTIFICATION_TEMPLATE`, `WHATSAPP_TEMPLATE_LANGUAGE=ar_AE`, `WHATSAPP_APP_SECRET`, `WHATSAPP_WEBHOOK_VERIFY_TOKEN`, `CRON_SECRET`, بالإضافة لمتغيرات سلة/Supabase الأصلية.

**جدول مرجعي — كل الملفات التي لُمست/راجعناها هذه الجلسة (يوفّر إعادة اكتشافها من الصفر):**

| الملف | الدور |
|---|---|
| `app/api/salla/webhook/route.ts` | استقبال `order.created` من سلة، تحقق HMAC، تشغيل المعالجة مع `await` (لا fire-and-forget) |
| `app/lib/order-processor.ts` | `processOrderInBackground`/`processSingleItem`، `lookupDesignerMapping`، `logRouting`، `createOrderRoutingSupabaseClient` |
| `app/lib/salla-types.ts` | تعريفات TypeScript لبنية payload سلة (`SallaOrderItem` مع `product.id` متداخل و`notes`) |
| `app/lib/whatsapp-cloud.ts` | `sendWhatsAppNotification`، بناء payload Meta، تصنيف أخطاء Meta المعروفة |
| `app/lib/whatsapp-template-variables.ts` | `buildTemplateVariables` — المتغيرات الأربعة لقالب `order_for_designer` (عُدِّل هذه الجلسة: عربي بدل إنجليزي) |
| `app/api/cron/refresh-salla-tokens/route.ts` | **جديد** — تجديد `access_token` اليومي |
| `app/api/whatsapp/webhook/route.ts` | **جديد** — استقبال حالات تسليم واتساب من Meta |
| `app/api/test-order-processor/route.ts` | endpoint اختباري بـSTUB — لا يُلمَس أبدًا (قاعدة قديمة ثابتة) |
| `app/api/test/name-extraction/route.ts` | endpoint اختباري لـregex الاستخراج — استخدم `unstable_noStore()` لا `dynamic='force-dynamic'` (Next.js 16 + cacheComponents) |
| `vercel.json` | `crons` (تجديد التوكن) + `rewrites` غير موثَّق المصدر (القسم 7) |
| `test-order-webhook.sh` | سكربت curl لاختبار `order.created` بحمولة مصطنعة موقَّعة — لا يزال صالحًا للاستخدام |

---

## 3) الحاجز الوحيد المتبقي أمام إطلاق حقيقي — رقم واتساب Sandbox

`WHATSAPP_PHONE_NUMBER_ID` الحالي يشير لرقم اختبار Meta الافتراضي (`+1 555-606-4662`، مُسمّى حرفيًا **"Test Number"** بلوحة Meta، تقييم جودة "مرتفع" لكن هذا لا يغيّر طبيعته كـSandbox). أرقام 555 محجوزة حصرًا للاختبار (موثَّق من Meta مباشرة)، ومقيّدة ببوابة موافقة/تفاعل من طرف المستلِم قبل قبول قوالب لاحقة — سبب خطأ Meta `131049` ("healthy ecosystem engagement") الذي ظهر فعليًا بطلب حقيقي.

**لماذا هذا حاسم:** أي مصمّم حقيقي جديد لن يرسل "مرحبا" لرقم أمريكي مجهول قبل استلام إشعارات طلباته. نجاح أي اختبار برقمك الشخصي بعد "موافقة" يدوية **لا يثبت جاهزية إطلاق** — فقط أن رقمًا واحدًا مسموح مؤقتًا.

**الحل (مؤجَّل عمدًا لاجتماع صديقك، صفر عمل كود):**
1. Meta App Dashboard → WhatsApp → API Setup → Add phone number → رقم حقيقي (يُفضَّل رقم أعمال يملكه/يتحكم به صديقك، اتساقًا مع الهيكل القانوني بالقسم 0) → تحقق OTP.
2. لا حاجة بالضرورة لـ"Business Verification" الكاملة فورًا — حتى الحد الافتراضي (250 محادثة/24 ساعة) يفوق حجم استخدامكم الفعلي، والأهم أنه يُخرج من قيود Sandbox المسبِّبة لـ131049 تحديدًا.
3. حدّث `WHATSAPP_PHONE_NUMBER_ID` بـVercel للـID الجديد → Redeploy. لا تعديل كود إطلاقًا.

---

## 4) فجوة مفتوحة — استخراج التخصيص من "خيار منتج"، لا الملاحظة فقط

طلب حقيقي فعلي أظهر أن العميل يُدخل نص التخصيص عبر **خيار منتج مُهيَّأ بسلة** ("خيار / بأسم: عبدالله")، لا حقل الملاحظة العام. الكود الحالي يقرأ `item.notes` حصرًا (مُصلَح هذه الجلسة من `item.note` الخاطئ أصلًا — لكن يبقى الحقل الخطأ من الأساس لهذا النمط تحديدًا من الاستخدام). النتيجة: `raw_note` فارغ، `personalization_detected=false`، `extracted_name=null` — **رغم** أن الإرسال نفسه ناجح (يعمل كالمصمَّم: لا تخصيص مكتشف ← إرسال كامل بلا شارة).

**لم يُبنَ حل بعد.** الخطوة التالية: جلب بنية `item.options[]` الفعلية من payload حقيقي محفوظ (راجع Vercel Logs لأي طلب اختبار سابق، أو حمولة `formak-order-created-test.json`)، ثم توسيع منطق الاستخراج ليفحص كلا المصدرين.

---

## 5) سجل الأخطاء المكتشفة بهذه الجلسة (استمرار ترقيم من #1-17 بـHandoff-3)

| # | الخطأ | الدرس |
|---|---|---|
| 18 | طلب لوحة تحكم يدوي **كامل** (إنشاء + دفع بنكي + تأكيد) في test_01 **أطلق** `order.created` فعليًا — يُصحّح جزئيًا خطأ #17 (الاختبار الأصلي استخدم زر "إنشاء" فقط بلا تسلسل كامل) | استبعاد سلوك منصة يحتاج تكرار التسلسل **الكامل** المستخدَم فعليًا من عميل حقيقي، لا خطوة واحدة معزولة |
| 19 | `item.product_id` مسطّح غير موجود بالحمولة الحقيقية — الحقل الفعلي متداخل `item.product.id` | لا تثق ببنية Salla قبل تأكيدها بتوثيقهم الرسمي مباشرة، حتى لو "منطقية" ظاهريًا |
| 20 | الكود قرأ `item.note` (مفرد) — سلة ترسل `notes` (جمع) | نفس الدرس، فرق حرف واحد كافٍ لفشل صامت كامل |
| 21 | Vercel serverless يقتل fire-and-forget بعد الرد مباشرة — معالجة الخلفية كانت تبدأ وتُقتل فورًا | أنماط "استجب فورًا، عالج لاحقًا" بلا `await`/`waitUntil` لا تعمل على serverless بديهيًا |
| 22 | متغير `to` غير معرَّف بنطاق دالة مختلفة — استُخدم بسطر `console.error` تشخيصي فقط، كسر البناء بالكامل | حتى تعديل تسجيل بسيط يحتاج فحص نطاق المتغيرات، لا نسخ/لصق من سياق مختلف |
| 23 | `WHATSAPP_DESIGNER_NOTIFICATION_TEMPLATE` لم يكن مضبوطًا بـVercel إطلاقًا — الكود يتوقعه، الاسم المخطَّط أصلًا (`WHATSAPP_TEMPLATE_NAME`) كان مختلفًا | فجوة تسمية بين خطة ما قبل التنفيذ (Phase 7-11) والتنفيذ الفعلي — تحقق من الاسم الحرفي بالكود لا الخطة |
| 24 | القالب معتمد بلغة `ar_AE`، لا `ar` الافتراضي — Meta رفضت بخطأ "template does not exist" | حالة الاعتماد **واللغة الحرفية** كليهما يحتاجان تحققًا مباشرًا من لوحة Meta، لا افتراضًا من الاسم |
| 25 | تغيّر `x-vercel-id` **لا يثبت** نجاح بناء نشر جديد — نشر بناء فاشل لا يزال يُغيّر الـID | الدليل الوحيد الموثوق: علامة ✓ خضراء بـGitHub/Vercel Dashboard مباشرة، لا أي header |
| 26 | `CRON_SECRET` **لا يُوفَّره Vercel تلقائيًا** (رغم ما ذُكر بجلسة سابقة اعتمادًا على مصدر ثانوي) — يُنشَأ يدويًا كأي متغير بيئة | حتى معلومة من Claude تحتاج تحقق من توثيق Vercel الرسمي مباشرة عند التعارض، لا الاكتفاء بمصدر واحد |
| 27 | PostgREST يُرجع أعمدة `bigint` كنص JSON، لا رقمًا — كسر type guard يتوقع `typeof === 'number'`، رفض كل الصفوف بصمت (`console.warn` غير بارز) | أي تحقق نوع runtime على عمود `bigint` قادم من Supabase JS مباشرة يحتاج `string`، لا `number` — بعكس نفس الحقل لو قادمًا من payload خارجي (سلة/Meta) |
| 28 | `WHATSAPP_PHONE_NUMBER_ID` يشير لرقم Sandbox (`+1 555`)، لا رقم أعمال حقيقي — سبب خطأ Meta `131049` المتقطع | القسم 3 أعلاه — لا يزال مفتوحًا |
| 29 | شارة `{{4}}` كانت إنجليزية بالكامل (`'None'` حرفيًا كـfallback مُسمّى، و`'Name customization requested'`) — لا تطابق الخطة الأصلية العربية | تحديث جزئي لملف عند تغيير الخطة (تحديث ثابت الملاحظة للعربية، نسيان ثابتَي الشارة) — تحقق من **كل** الثوابت بملف عند أي تغيير خطة، لا الحقل المستخدَم مباشرة فقط |
| 30 | عميل حقيقي أدخل التخصيص عبر "خيار منتج" لا حقل الملاحظة العام | القسم 4 أعلاه — لا يزال مفتوحًا |

---

## 6) ملاحظات نمط العمل

- **Trae AI:** نفد حد الاستخدام المجاني (~$1) هذه الجلسة — تحوّل الاعتماد الأساسي لـ**MiniMax M3 (Code Desktop)**.
- **M3:** انضباط جيد عمومًا، يعرض diff حقيقي عند الطلب المباشر. نمط خفيف متكرر (3 مرات هذه الجلسة) من التباس مخرجات الطرفية بـWindows/PowerShell (تشويه ترميز عربي/emoji، التباس git diff مع ملف untracked سابقًا، وعدد صفحات بناء غير متسق مرة واحدة) — لم يسبب ضررًا فعليًا أي مرة (تعافى ذاتيًا أو تحقَّق يدويًا لاحقًا)، لكن يستحق استمرار التحقق المستقل من محتوى الملف الفعلي لا الوصف فقط، تحديدًا حول أوامر git/الطرفية بهذا النظام.
- **Gemini:** يُستخدَم كوسيط موازٍ (المستخدم يلصق ردود Claude له، يحصل على رأيه، أحيانًا يعيد صياغتها كبرومبتات لـM3). هذه الجلسة: تحليله لـ"accepted≠delivered" كان صحيحًا (متطابقًا مع استنتاج Claude المستقل)، لكن تفسيره الدقيق لآلية خطأ 131049 (نافذة الـ24 ساعة الكلاسيكية) كان غير دقيق على الأرجح — الآلية الأصح موثَّقة بالقسم 3 (خاصية Sandbox تحديدًا، لا القاعدة العامة). استمر بالتحقق من أي ادّعاء تقني محدد منه قبل التنفيذ.

---

## 7) قائمة سريعة لكل ما هو مفتوح الآن

- [ ] **الأولوية القصوى:** ربط رقم واتساب أعمال حقيقي (القسم 3) — بانتظار اجتماع صديقك
- [ ] استخراج التخصيص من `item.options[]` بالإضافة لـ`item.notes` (القسم 4)
- [ ] توثيق الاتفاق المكتوب مع صديقك
- [ ] توضيح عمود `whatsapp_number` غير المُوثَّق بجدول `merchants`
- [ ] تنظيف صفّي merchants الوهميين (`salla_store_id` 999999 و12345 — بقايا اختبار سباق التوقيت بالمرحلة 9)
- [ ] فحص `vercel.json` → مفتاح `rewrites` (`"/(.*)" → "/index.html"`) نمط SPA تقليدي لا يخص Next.js App Router، مصدره غير موثَّق، لا يبدو أنه يعطّل شيئًا حاليًا (كل المسارات الفعلية تعمل) — يستحق فهمًا لا حذفًا عشوائيًا
- [ ] لاحقًا: صفحة "اربط متجرك" لحل فجوة `user_id`
- [ ] **تجميع الطلبات اليومي بالفئة/الكود** — القرارات الثلاثة اتُّخذت هذه الجلسة (القسم 8 أدناه: عمود يدوي، اختياري لكل فئة، صباحًا بتوقيت الرياض)، **لم يُبنَ بعد**

---

## 8) ملحق: تصميم أولي لتجميع الطلبات اليومي — قيد النقاش، غير نهائي

**الفكرة (من صديقك، مطروحة أصلًا بـHandoff-3 §6، أُعيد إحياؤها هذه الجلسة كأولوية):** بدل رسالة واتساب فورية لكل طلب، جمع كل طلبات فئة/كود منتج معيّن (مثال: `b150`) خلال يوم كامل في رسالة واحدة موحَّدة للمصمم — يحاكي فائدة نمط "قروب لكل صنف" المستخدَم بمتاجر منافسة (بلا استخدام WhatsApp Groups الممنوع فعليًا بمشروعك).

**الشكل التقني المتوقَّع (لم يُبنَ، تصوّر أولي فقط):**
1. عمود جديد `category_code text` بـ`product_designer_map` — يربط عدة `salla_product_id` مختلفة بنفس فئة التجميع.
2. حالة جديدة بـ`whatsapp_status` (مثال: `pending_batch`) — الطلب يُسجَّل فور وصوله كالمعتاد، لكن **لا يُرسَل فوريًا** إن كان منتجه ضمن فئة مُجمَّعة.
3. **قالب Meta جديد كليًا** — القالب الحالي (`order_for_designer`) مصمَّم لطلب واحد فقط (4 متغيرات ثابتة)، لا يصلح لقائمة متعددة الطلبات. يحتاج تقديمًا ومراجعة Meta منفصلة (ساعات-يومان عادة حسب تجربتكم بالمرحلة 8).
4. Cron يومي جديد (نفس نمط `refresh-salla-tokens` المُثبَت هذه الجلسة) — يجمع كل صفوف `pending_batch` حسب `(designer_whatsapp, category_code)`، يبني رسالة واحدة، يرسل، يحدّث الحالة.

**3 قرارات تصميمية اتُّخذت بنفس جلسة الطرح — جاهزة كمواصفة لأي برومبت لاحق:**

1. **مصدر كود الفئة:** عمود يدوي جديد `category_code text` (nullable) بـ`product_designer_map` — لا اعتماد على SKU سلة (خطر إملائي/انضباط تسمية يكسر التجميع بصمت بلا تحذير).
2. **النطاق: اختياري لكل فئة، لا استبدال كامل.** `category_code IS NULL` ← فوري كالحالي تمامًا (المسار المُثبَت حيًا هذه الجلسة، **لا يُمَس إطلاقًا**). `category_code IS NOT NULL` ← يدخل مسار التجميع. هذا يحصر خطر أي كود جديد بالفئات المُفعَّلة صراحة فقط.
3. **التوقيت: صباحًا، يشمل يوم أمس كاملاً.** ⚠️ **تحذير تقني حرج:** يجب حساب حدود "أمس" بتوقيت **Asia/Riyadh (UTC+3)**، لا UTC الخام — `now() - interval '1 day'` بـPostgres مباشرة يستخدم UTC، يُزيح حدود اليوم بمقدار 3 ساعات (يُدخل آخر 3 ساعات من اليوم الحالي بالخطأ، أو يُسقط أول 3 ساعات من أمس). احسب `(now() at time zone 'Asia/Riyadh')::date - 1` صراحة، لا حسابًا ضمنيًا بـUTC.

**غير مبني بعد — قرار متعمَّد، ليس نسيانًا.** ينتظر إما بدء التنفيذ الفعلي بالمحادثة الجديدة، أو حل حاجز رقم Sandbox أولًا (القسم 3) — قرارك.
